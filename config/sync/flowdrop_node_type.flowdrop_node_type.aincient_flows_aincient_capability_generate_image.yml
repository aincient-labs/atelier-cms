uuid: 9d1e5b4a-6c2f-4a8e-b1d7-2f0c3e9a7b64
langcode: en
status: true
dependencies:
  module:
    - aincient_flows
id: aincient_flows_aincient_capability_generate_image
label: 'Capability: Generate image'
description: 'Generate a NEW image with the site''s image model, or EDIT an existing one. Pass "prompt" alone to create from scratch (text→image); ALSO pass "source" as a media:<id> token to edit an existing image (image→image). Always creates a new media item + token (never overwrites) and returns it inline plus in the Library.'
category: processing
icon: 'mdi:image-plus'
plugin_version: 1.0.0
enabled: true
parameters:
  prompt:
    configurable: true
    connectable: true
    required: true
  source:
    configurable: true
    connectable: true
    required: false
outputs:
  ok:
    exposed: true
  result:
    exposed: true
  tool:
    exposed: true
tags: {  }
executor_plugin: 'aincient_flows:aincient_capability:generate_image'
visual_type: default
supported_visual_types:
  - default
  - tool
unified_input_exposed: false
unified_output_exposed: false
tool_exposed: false
