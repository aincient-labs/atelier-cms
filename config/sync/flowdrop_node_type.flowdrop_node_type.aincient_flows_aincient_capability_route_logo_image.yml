uuid: 863d2cfa-83ff-4781-a325-e69b485bf0e1
langcode: en
status: true
dependencies:
  module:
    - aincient_flows
id: aincient_flows_aincient_capability_route_logo_image
label: 'Capability: Route logo image to Identity'
description: 'Hand the operator off to the Identity studio''s Logo field when they ATTACHED an image this turn and want it used as the site LOGO, brand mark, wordmark, or FAVICON. Images cannot be placed from chat, so call this INSTEAD of trying to apply it: it shows a card that opens Identity → Logo, where they drop the file into the picker. Pass asset="favicon" for the browser-tab icon, otherwise "logo". It places nothing.'
category: tools
icon: 'mdi:image-outline'
plugin_version: 1.0.0
enabled: true
parameters:
  asset:
    configurable: false
    connectable: true
    exposed_by_default: true
    required: false
outputs:
  ok:
    exposed: true
    exposed_by_default: true
  result:
    exposed: true
    exposed_by_default: true
tags: {  }
executor_plugin: 'aincient_flows:aincient_capability:route_logo_image'
visual_type: tool
supported_visual_types:
  - default
  - tool
unified_input_exposed: false
unified_output_exposed: false
tool_exposed: false
reserved_port_exposure: {  }
