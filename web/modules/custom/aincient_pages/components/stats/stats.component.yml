name: Stats
status: experimental
group: 'AIncient Landing'
description: 'A row of headline numbers. Great for social proof under a hero.'
props:
  type: object
  properties:
    tone:
      type: string
      enum: [default, muted, brand, inverted]
      default: muted
    items:
      type: array
      description: 'Array of { value, label }.'
thirdPartySettings:
  atelier:
    api: 1
    tier: section
    order: 30
    icon: ▦
    use: 'A row of headline numbers; great social proof under a hero.'
    props:
      tone: ''
      items: '[{value,label}]'
