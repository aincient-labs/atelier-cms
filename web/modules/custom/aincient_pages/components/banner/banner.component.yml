name: Banner
status: experimental
group: 'AIncient Landing'
description: 'A short, bold full-width statement band — one big sentence with an optional action.'
props:
  type: object
  properties:
    tone:
      type: string
      enum: [default, muted, brand, inverted]
      default: brand
    eyebrow: { type: string }
    heading: { type: string }
    subheading: { type: string }
    cta_label: { type: string }
    cta_url: { type: string }
thirdPartySettings:
  atelier:
    api: 1
    tier: section
    order: 140
    icon: ▬
    use: 'A short, bold full-width statement band — one big sentence (a mission line or pull-quote). One idea, no list.'
    props:
      tone: ''
      eyebrow: ''
      heading: ''
      subheading: ''
      cta_label: ''
      cta_url: ''
